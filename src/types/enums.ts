export enum transactionStatuses {
  "pending" = "pending",
  "signed" = "signed",
  "failed" = "failed",
  "cancelled" = "cancelled",
}

export enum loginMethodsEnum {
  ledger = 'ledger',
  walletconnect = 'walletconnect',
  wallet = 'wallet',
  extension = 'extension',
  none = ''
}
