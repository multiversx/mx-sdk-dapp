name: Publish sdk-dapp

on:
  push:
    branches: [main]
  repository_dispatch:
    types: publish-npm
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish-npm:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/checkout@v3
      - name: Get package info
        id: package
        uses: andreigiura/action-nodejs-package-info@v1.0.2
      - run: |
          echo "result ${{ steps.package.outputs.is-prerelease }}"
          echo "is-release-candidate: ${{ steps.package.outputs.is-release-candidate }}"
